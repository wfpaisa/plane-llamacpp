/* about.ts
 *
 * Release notes shown in the preferences window.
 *
 * SPDX-License-Identifier: GPL-2.0-or-later
 */

export const releaseNotes = `
Version 1 - What's new:

•  Custom command menu in the GNOME top bar: add display names and shell
   commands, reorder them by drag and drop, and show/hide entries.
•  Separators (--- or ~~~), optional labeled separators, and submenus (start a
   name with *).
•  Dynamic labels using $(command) substitution in names.
•  Special command ##ShowApplications to open the application overview.
•  Import/export the command list to a commands.ini file, and reset to defaults.
`;
